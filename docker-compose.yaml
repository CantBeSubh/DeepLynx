services:
    app:
        env_file:
            - .env
        build:
            context: ./
            dockerfile: Dockerfiles/Dockerfile.app.local
        ports:
            - 3000:3000
        environment:
           NODE_ENV: production
    backend:
        env_file:
            - .env
        build:
            context: ./
            dockerfile: Dockerfiles/Dockerfile.backend.local
        ports:
            - 5000:5000
        entrypoint: /usr/local/bin/entrypoint.sh